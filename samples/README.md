# Sample Images

Run this command to create simple PNG examples:

```bash
python scripts/create_sample_images.py
```

Use real meal photos for the final submission screenshots because synthetic samples are only for smoke testing the file handling path.
